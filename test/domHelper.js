'use strict';

var expect = require( 'chai' )
	.expect;
var domHelper = require( '../index' );

describe( 'domHelper', function () {
	it( 'test', function () {
		var result = 1;
		expect( result )
			.to.equal( 1 );
	} );
} );
